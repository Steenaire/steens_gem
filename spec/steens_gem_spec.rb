require 'spec_helper'

describe SteensGem do
  it 'has a version number' do
    expect(SteensGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
